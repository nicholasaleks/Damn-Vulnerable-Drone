services:
  flight-controller:
    image: n1ckaleks/damn-vulnerable-drone-flight-controller:latest
    container_name: flight-controller
    privileged: true
    build:
      context: .
      dockerfile: ./flight-controller/Dockerfile
    volumes:
      - serial-uart-socket:/sockets
    networks:
      simulator:
        ipv4_address: 10.13.0.2

  companion-computer:
    image: n1ckaleks/damn-vulnerable-drone-companion-computer:latest
    container_name: companion-computer
    privileged: true
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "3000:3000"
    build:
      context: .
      dockerfile: ./companion-computer/Dockerfile
    depends_on:
      - flight-controller
    volumes:
      - serial-uart-socket:/sockets
    environment:
      - WIFI_ENABLED
      - ROS_DOMAIN_ID=42
      - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
      - CYCLONEDDS_URI=file:///etc/cyclonedds.xml
    networks:
      simulator:
        ipv4_address: 10.13.0.3

  ground-control-station:
    image: n1ckaleks/damn-vulnerable-drone-ground-control-station:latest
    container_name: ground-control-station
    privileged: true
    environment:
      - WIFI_MODE
      - DISPLAY=${DISPLAY}
      - QT_NO_MITSHM=1
      - XDG_RUNTIME_DIR=/tmp
      - XAUTHORITY=/home/gcs/.Xauthority
      - HEADLESS=0
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ${HOME}/.Xauthority:/home/gcs/.Xauthority:ro
      - /etc/machine-id:/etc/machine-id:ro
    devices:
      - /dev/dri:/dev/dri
    build:
      context: .
      dockerfile: ./ground-control-station/Dockerfile
    depends_on:
      - companion-computer
    networks:
      simulator:
        ipv4_address: 10.13.0.4

  simulator:
    image: n1ckaleks/damn-vulnerable-drone-simulator:latest
    container_name: simulator
    # Pinned to linux/amd64 because Gazebo Classic 11 has no apt-
    # accessible arm64 build for Jammy (OSRF dropped arm64 around EOL;
    # Ubuntu universe is amd64-only). On Apple Silicon hosts this runs
    # under Docker Desktop's Rosetta 2 amd64 emulation.
    platform: linux/amd64
    privileged: true
    ulimits:
      nofile:
        soft: 1024
        hard: 524288
    build:
      context: .
      dockerfile: ./simulator/Dockerfile
      platforms:
        - linux/amd64
    ports:
      - "8080:8080" # gzweb
      - "8000:8000" # simulator mgmt
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./simulator/mgmt:/Simulator/mgmt
    environment:
      - WIFI_ENABLED
      - LITE=false
      - ROS_DOMAIN_ID=42
      - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
      - CYCLONEDDS_URI=file:///etc/cyclonedds.xml
    networks:
      simulator:
        ipv4_address: 10.13.0.5

volumes:
  serial-uart-socket:
  ardupilot:

networks:
  simulator:
    name: simulator
    internal: false
    driver: bridge
    ipam:
      config:
        - subnet: "10.13.0.0/24"
